// Count the frequency of each character in a string.

let str = 'Urvi';
let count = {};

for(let i=0;i<str.length;i++){
  if(count[str[i]]){
    count[str[i]]++;
  }else{
    count[str[i]] = 1;
  }
}

console.log(count);
